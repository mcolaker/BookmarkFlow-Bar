# BookmarkFlow Bar Açık İşler

Son güncelleme: 2026-08-05

Otorite: Bu dosya kanonik durum ve kanıt kaydıdır. Proje çalışma kuralları için [AGENTS.md](../../AGENTS.md) otoritedir.

## Kapanış sözleşmesi

`OPEN` çalışmaya hazır, `IN_PROGRESS` uygulaması veya doğrulaması başlamış, `BLOCKED` ise cihaz, kullanıcı kararı, dış hesap/yetki veya harici kanıt bekleyen iştir. Bir görev yalnız kod yazıldığı, kısmen ilerlediği veya başka işe geçildiği için kapanmaz. Kabul kriteri ve doğrulama kapısı somut kanıtla geçmeden `DONE` yapılamaz. Her kullanıcı işinin başında ve sonunda bu dosya okunur; yeni bulgular eklemeli yazılır, açık ya da tamamlanan kayıtlar kanıtsız silinmez.

## P1 - Ürün doğruluğu ve yönetişim

## BF-GOV-001 - Kanonik backlog ve kanıt sözleşmesini kur

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: Projede `AGENTS.md`, kanonik görev defteri ve makinece doğrulanan durum geçişi bulunmuyordu; `AGENTS.md`, bu backlog, fail-closed validator, negatif regresyon testleri ve CI kapısı eklendi.
- Kabul kriteri: Tek kanonik backlog, stabil görev kimlikleri, izinli durumlar, zorunlu kanıt/kabul/gate/sonraki-adım alanları ve başlangıç-kapanış uzlaştırması proje talimatlarında tanımlandı.
- Doğrulama kapısı: `node scripts/validate-backlog.mjs` ve `node --test scripts/backlog-contract.test.mjs` temizdir; CI aynı kapıları zorunlu çalıştırır.
- Sonraki adım: Yok; yeni veya yeniden açılan işler stabil `BF-*` kimliğiyle bu dosyaya eklenir.
- Son güncelleme: 2026-08-02.

## BF-I18N-001 - İngilizce/Türkçe yerelleştirme geçişini kanıtla

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: Ürün arayüzü Türkçe sabit metinlere bağlıydı ve global tanıtım için tarayıcı diline göre İngilizce/Türkçe sunum gerekiyordu. `_locales/`, `src/i18n.js`, yerelleştirilmiş manifest ve tüm ana UI yüzeyleri eklendi. 2026-08-04 regresyon denetimi `content.js`, `background.js` ve `newtab.js` içinde İngilizce arama ve klasör eşlemesini de `tr-TR` harf kurallarına zorlayan sabit normalizasyonu kanıtladı; `INSURANCE` başlığı `insurance` sorgusuyla eşleşmiyordu. Türkçe mağaza metninde de gerçek Türkçe karakterler yerine ASCII transliterasyon bulunuyordu. Etkin Chrome dili `en-US`/`tr-TR` normalizasyonuna bağlandı; runtime/UI sözleşmeleri İngilizce ve Türkçe arama/klasör davranışını geçti, Türkçe mağaza metni yenilendi ve bağımsız İngilizce/Türkçe geçici gerçek Chrome güvenlik regresyonları temiz sonuç verdi.
- Kabul kriteri: Manifest varsayılan locale'i, bütün kullanılan çeviri anahtarları, İngilizce/Türkçe anahtar paritesi, güvenli fallback, popup/new-tab/onboarding/content/settings yüzeyleri ve erişilebilir metinler tutarlıdır; arama ve klasör tekilleştirme etkin dile göre `en-US` veya `tr-TR` normalizasyonu kullanır; Türkçe mağaza metni gerçek Türkçe karakterlerle İngilizce özellik kapsamına paraleldir.
- Doğrulama kapısı: İngilizce `I/i` ve Türkçe `İ/ı` davranış testleri, `en`/`tr` güvenlik-regresyon matrisi, `validate-project` locale paritesi, public-tree ve gerçek Chrome iki dil smoke kapıları temizdir. 2026-08-04 son yerel koşuda birleşik runtime/UI sözleşmeleri 13/13, manifest `0.1.38` locale doğrulaması ve 96 dosyalık public-tree taraması geçti; bağımsız geçici gerçek Chrome `en`/`tr` matrisi de temiz sonuç verdi.
- Sonraki adım: Yok; yeni locale veya kullanıcıya görünen metin eklendiğinde anahtar paritesi ve iki dilli Chrome matrisi birlikte güncellenir.
- Son güncelleme: 2026-08-04.

## BF-CI-001 - Linux CI yerelleştirme matrisini locale destekli Chrome ile doğrula

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: PR #21 için 2026-08-05 tarihli `Validate extension` çalışması `31030981955` içinde statik doğrulama ve İngilizce tarayıcı regresyonu geçti; Türkçe regresyon `Chrome locale mismatch: requested tr, got en` hatasıyla durdu ve toplu `validate` işi yalnız bu nedenle başarısız oldu. Playwright `1.55.0` Chromium'u Linux'ta yalnız `--lang=tr` ile başlatıldığında sistem `LANGUAGE` değeri İngilizce kaldığı için uzantıya İngilizce UI dili bildirdi. İlk düzeltme denemesi `31031625871`, locale paketleri bulunan `/usr/bin/google-chrome` yolunu başarıyla doğruladı; ancak güncel markalı Chrome command-line unpacked extension yüklemesini engellediği için hem İngilizce hem Türkçe işinde `BookmarkFlow service worker did not start` sonucu verdi. Son düzeltme Playwright Chromium'u koruyup Linux browser sürecine matrix diliyle `LANGUAGE=en|tr` aktardı ve daha yüksek öncelikli `LC_ALL`/`LC_MESSAGES` değerlerini temizledi. PR #21 çalışması `31031878372` statik doğrulama, İngilizce ve Türkçe tarayıcı regresyonları ile toplu `validate` işlerinin tamamında terminal `success` verdi.
- Kabul kriteri: GitHub tarayıcı matrisi sabit `ubuntu-24.04` imajında command-line extension yüklemeyi destekleyen exact Playwright `1.55.0` Chromium'u kullanır; Linux browser süreci istenen matrise göre `LANGUAGE=en|tr` ile ve daha yüksek öncelikli `LC_ALL`/`LC_MESSAGES` override'ları temizlenerek başlatılır; markalı Google Chrome yolu testte kullanılmaz; İngilizce ve Türkçe güvenlik/yerelleştirme işleri ile toplu `validate` terminal `success` verir.
- Doğrulama kapısı: Kaynak doğrulaması sabit Ubuntu imajını, exact Playwright Chromium kurulumunu, matrix dil aktarımını ve Linux `LANGUAGE` override'ını zorunlu tutup markalı Chrome yolunu reddeder; yerel backlog/proje/public-tree ile İngilizce/Türkçe gerçek Chrome güvenlik kontrolleri geçer. GitHub Actions `31031878372` çalışmasında `Static validation`, `Browser regression (en)`, `Browser regression (tr)` ve toplu `validate` işlerinin dördü de terminal `success` verdi.
- Sonraki adım: Yok; Playwright veya Ubuntu runner sürümü değiştiğinde aynı iki dilli tarayıcı matrisi korunur.
- Son güncelleme: 2026-08-05.

## BF-EXT-001 - Sayfa içi çubuğun hedef Chrome profilinde yüklenmesini doğrula

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: Önceki tanıda panel toolbar popup değil content script tarafından oluşturuluyordu. Kullanıcı ekran görüntülerinde çubuk ve klasör rayı artık görünür; güncel kaynakla geçici gerçek Chrome profilinde content script stili, kapalı Shadow DOM, güvenli ekleme akışı ve yerel host tercihi regresyonu geçmiştir. 2026-08-04 çift-kök yeniden üretiminde `getBookmarkData()` hesap ve yerel ray klasörlerini üretmesine rağmen `getState()` bu alanı yanıta taşımadı; `selectBookmarkBarNode()` birden fazla `bookmarks-bar` düğümünde `syncing` hesap kökünü tercih etmek yerine ilk düğümü seçti. Sonuç Google hesabı klasörlerinin kaybolması ve varsayılan yazma/yeniden sıralama hedefinin yerel bara sapmasıydı.
- Kabul kriteri: Güncel kaynak unpacked olarak yüklenir; sıradan izinli HTTPS sayfasında kök ve stiller görünür, genel kapatma/devre dışı host/hassas host durumları ayrı ve anlaşılır sonuç verir; durum yanıtı hesap ve yerel ray klasörlerini eksiksiz taşır; görüntüleme, ekleme ve yeniden sıralama hesap `bookmarks-bar` düğümünü deterministik seçip hesap düğümü yoksa güvenli yerel fallback kullanır.
- Doğrulama kapısı: Sentetik çoklu hesap/yerel bookmark kökü regresyonu, iki dil güvenlik regresyonu, yeni bookmark ve yeniden sıralama hedefi, normal/kapalı/devre dışı/hassas host matrisi ve gerçek Chrome reload kanıtı temizdir. Otomatik çift-kök seçimi, durum taşıması ve local fallback sözleşmeleri geçti; oturumlu kullanıcı profilinde hesap köküne gerçek yazma/yeniden sıralama kanıtı henüz `not_run` durumundadır.
- Sonraki adım: Kod ve sentetik çift-kök regresyonu tamamlandı; kullanıcı oturumlu gerçek Chrome profilinde hesap ve cihaz-yerel köklerin birlikte göründüğünü, yeni yer iminin hesap çubuğuna yazıldığını ve yeniden sıralamanın aynı hedefi kullandığını doğrula. Bu kullanıcı profili kanıtı gelene kadar görev `BLOCKED` kalır.
- Son güncelleme: 2026-08-04.

## BF-REL-001 - Profesyonel global GitHub sürümünü yayımla

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: Genel repo kaynak kodu içeriyordu ancak global ürün sunumu, kalıcı gizlilik URL'si, erişilebilir iki dilli arayüz, açık lisans duruşu, korumalı ana dal ve indirilebilir sürüm paketi eksikti. Önceki sürüm kapıları tamamlandı; 2026-08-04 denetimi paketleyicinin varsayılan olarak hareketli `HEAD`/dal ref'i kabul ettiğini ve `.gitattributes` export-ignore arşiv kapsamının negatif sözleşme testiyle korunmadığını gösterdi. 2026-08-05 tarihinde kullanıcı yalnız bu proje için `0.1.38` commit, sürüm etiketi, ZIP ve SHA-256 üretimine açık onay verdi. Aynı gün exact-tag paketleme zinciri, DCO `Signed-off-by` commit'i ve açıklamalı etiket için yerel preflight tamamlandı; Git yapılandırmasında GPG/SSH imza anahtarı veya `gpg` çalıştırıcısı bulunmadığından etiket kriptografik olarak imzalanamaz ve bu sınır kullanıcıya açıkça bildirilecektir.
- Kabul kriteri: Kapsamlı İngilizce ürün sayfası, gizlilik/destek rotaları, açık lisans bildirimi, mağaza metinleri, doğrulanmış sürüm ZIP'i ve SHA-256 özeti yayımlanır; yalnız `BookmarkFlow-Bar` reposunun `main` dalı korunur; paketleyici yalnız manifest sürümüyle birebir eşleşen değişmez `v<semver>` etiketi kabul eder ve arşiv allow/deny kapsamı fail-closed test edilir.
- Doğrulama kapısı: Hareketli `HEAD`/dal ref'leri negatif testte reddedilir; exact-tag arşivinin manifest, etiket, LICENSE/NOTICE/TRADEMARKS ve export-ignore kapsamı sözleşme testinden geçer; proje/public-tree/açık-kaynak/backlog/güvenlik kapıları temizdir. 2026-08-05 son preflight'ında birleşik runtime/UI/paket sözleşmeleri 23/23, backlog sözleşmeleri 5/5, manifest `0.1.38` proje doğrulaması, 96 dosyalık public-tree taraması ve İngilizce/Türkçe gerçek geçici Chrome güvenlik matrisi geçti. Bu kaynak commit'inden açıklamalı `v0.1.38` etiketiyle üretilecek ZIP ve SHA-256 yerel `dist/` tesliminde doğrulanır; kriptografik tag imzası yapılandırılmış anahtar bulunmadığı için `not_available` durumundadır.
- Sonraki adım: Yerel DCO commit'i, açıklamalı `v0.1.38` etiketi ve exact-tag ZIP/SHA tesliminden sonra kullanıcı isterse etiketi ve GitHub Release varlıklarını uzak repoya yayımlar. Uzak push/release yapılmadığından görev `BLOCKED` kalır.
- Son güncelleme: 2026-08-05.

## BF-OSS-001 - Codex for Open Source uygunluk ve benimsenme kanıtını oluştur

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: OpenAI Codex for Open Source programı aktif açık kaynak projelerin birincil veya çekirdek bakım sorumlularını; anlamlı kullanım, ekosistem önemi ve sürekli bakım kanıtlarıyla değerlendiriyor. Hak sahibi 2026-08-02 tarihinde `Apache-2.0 + ayrı marka politikası + inbound=outbound/DCO` modelini açıkça onayladı. Resmî Apache-2.0 ve DCO 1.1 metinleri, `NOTICE`, marka, yönetişim, yol haritası, destek ve katkı sözleşmeleri; PR commit aralığını fail-closed denetleyen CI; 14/14 binary varlık için boyut/SHA-256 köken kaydı ve sentetik verilerle yeniden üretilmiş beş pazarlama görseli hazırlandı. Açık kaynak kontratı 14/14, backlog kontratı 5/5, manifest `0.1.37` proje doğrulaması, 80 dosyalık public-tree ve İngilizce/Türkçe gerçek Chrome güvenlik regresyonları temizdir. PR #12'nin `validate` işi terminal `success` verdi; PR `79f500f` merge commit'iyle korumalı `main` dalına birleştirildi ve aynı commit'in `main` doğrulaması da terminal `success` oldu. `v0.1.37` etiketi bu merge commit'ine bağlıdır; GitHub Release içindeki ZIP `uploaded` durumunda ve GitHub asset digest'i `414ca7bbaf5c82999a39822c0759f3e968224af61bc30ab31fab1a00c2f19e47` değeriyle yerel SHA-256 özetini doğruluyor. GitHub lisansı `Apache-2.0` olarak tanıyor; Pages ürün ve gizlilik rotaları HTTPS `200` döndürüyor; Issues ve Discussions etkindir. ImgBot PR #2 birleştirilmeden kapatıldı ve uzak `imgbot` dalı silindi. Yayın anındaki canlı kanıt 3 yıldız, 2 izleyici, 0 fork, 0 açık issue/PR ve `v0.1.37` varlıklarında 0 indirme gösterdiğinden organik dış kullanım, sürdürülmüş bakım etkinliği, programa kabul ve altı aylık fayda etkinleştirmesi henüz kanıtlanmış değildir.
- Kabul kriteri: Hak sahibinin açık onayıyla gerçek bir açık kaynak lisans modeli seçilir; üçüncü taraf katkı ve görsel kaynak sahipliği çözülür; kod lisansı ile marka/resmî sürüm sınırı, gelecek katkıların inbound lisansı ve bakım yönetişimi açıkça belgelenir; doğrulanmış açık kaynak sürümü yayımlanır; mağaza kullanıcıları, sürüm indirmeleri, dış issue/PR/Discussion katkıları ve sürdürülen release faaliyeti gibi organik benimsenme kanıtları tarihli olarak kaydedilir; başvuru doğru ve eksiksiz bilgilerle gönderilir ve program kabulü ile fayda etkinleştirmesi kanıtlanır.
- Doğrulama kapısı: Seçilen lisans GitHub tarafından beklenen SPDX kimliğiyle tanınır; lisans/marka/katkı belgeleri birbiriyle çelişmez; public-tree, proje, backlog ve güvenlik regresyonları temizdir; yayımlanan kaynak etiketi ile mağaza paketi eşleşir; başvuru ölçümleri yeniden üretilebilir kamusal veya yönetici paneli kanıtına dayanır; OpenAI seçim ve etkinleştirme durumu son kullanıcıya ait gizli değerler yayımlanmadan doğrulanır.
- Sonraki adım: Chrome Web Store kararı ve temiz mağaza kurulum kanıtı tamamlandıktan sonra mağaza kullanıcılarını; GitHub release indirmelerini; dış issue, PR ve Discussion katkılarını; düzenli bakım ve sonraki release faaliyetini tarihli olarak izle. Organik kullanım ve bakım kanıtı anlamlı hale geldiğinde resmî Codex for Open Source formuna yalnız doğrulanmış ölçümlerle başvur; OpenAI seçim ve fayda etkinleştirme kanıtı gelene kadar görevi açık tut.
- Son güncelleme: 2026-08-02.

## BF-STORE-001 - Chrome Web Store inceleme gönderimini tamamla

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: Kullanıcı `v0.1.35` paketini doğru BookmarkFlow Bar kaydına yükledi; yayın öncesi politika denetiminde yeni sekme aramasının Google'a sabitlendiği ve Chrome Web Store'un varsayılan arama tercihini korumak için Chrome Search API kullanımını zorunlu tuttuğu doğrulandı. `v0.1.36` bu davranışı düzeltti; mağaza girişi, gizlilik, dağıtım, test talimatları, `search` izin gerekçesi ve sağlayıcıdan bağımsız ekran görüntüsü güncellendi. Dashboard geniş ana makine izninin ayrıntılı inceleme gerektireceğini bildirdi; bu izin sıradan web sayfalarındaki otomatik çubuk gösterimi için korunarak kullanıcı uyarıyı kabul etti ve 2026-08-02 tarihinde incelemeye gönderdiğini doğruladı. Sonrasında GitHub kanonik sahibi `mcolaker` oldu; önceki `09mc.github.io` gizlilik URL'si 404 verdiği için dashboard'daki gizlilik ve destek URL'lerinin yeni kanonik adreslerle uzlaştırılması gerekiyor. 2026-08-04 denetimi incelemedeki `0.1.36` paketinin 404 veren eski homepage'i ve eski proprietary lisans kimliğini taşıdığını, güncel Apache paketindeki `NOTICE`/`TRADEMARKS.md` dosyalarını içermediğini; reviewer izin özetinin de manifestteki `search` iznini atladığını kanıtladı. 2026-08-04 tarihinde kullanıcı mağaza kaydının `https://chromewebstore.google.com/detail/bookmarkflow-bar/iaikobkolclhhpcogacjkenijlfaibpf` adresinde yayımlandığını doğruladı. Web aracı aynı URL'yi `URL ... is not safe to open` güvenlik engeliyle açamadığı için canlı sayfanın sürüm, bağlantı ve gizlilik alanları bu turda bağımsız okunamadı. Yerel `0.1.38` güncellemesi reviewer/izin beyanlarını, iki veri kategorisini, Limited Use açıklamasını ve yalnız ZIP yükleme prosedürünü güncelliyor. 2026-08-05 tarihinde kullanıcı Web Store gizlilik ekranında **Web history**, **Website content** ve üç Limited Use beyanının davranışla uyumunu kontrol etmeye açık onay verdi; Chrome bağlantısı Google'ın yeniden kimlik doğrulama ekranına ulaştı ve kullanıcı oturumu beklenirken kaynak/politika karşılaştırması sürüyor.
- Kabul kriteri: Chrome Search API kullanıcının mevcut varsayılan arama motorunu korur; izin, iki dil, mağaza açıklamaları, reviewer notları, gizlilik politikası ve paket içindeki açık-kaynak/kanonik URL kimliği davranışla tutarlıdır; tüm yerel P1 düzeltmelerini içeren yeni patch ZIP aynı mağaza kaydına yüklenir; dashboard gizlilik ve destek URL'leri çalışan `mcolaker` kanonik adreslerini kullanır; kullanıcı nihai gönderimi onayladıktan sonra inceleme durumu kanıtlanır.
- Doğrulama kapısı: Backlog sözleşmesi, proje doğrulaması, public-tree, iki dilde güvenlik regresyonu ve gerçek Chrome'da varsayılan arama sağlayıcısı akışı temizdir. PR #5 `validate` kontrolü terminal `success` verdi ve `main` dalına birleştirildi; GitHub Pages gizlilik rotası Limited Use metniyle HTTPS `200` döndürdü. `v0.1.36` release ZIP'i ve SHA-256 dosyası `uploaded` durumda ve GitHub asset digest'i `d53fed4685fbf4b19c879a9180c08623d08a5f751ab0f3e5fdb649c809da9a33` değeriyle yerel paketi doğruluyor. Dashboard'a `0.1.36` yüklendi ve kullanıcı gönderimi tamamladığını bildirdi; 2026-08-04 kullanıcı teyidi aynı uzantı kimliğinin artık canlı olduğunu kanıtlıyor. 2026-08-05 kaynak doğrulaması **Web history**, **Website content**, **User activity seçilmemeli** ve üç Limited Use beyanını davranışla eşleştirdi; proje validator'ü bu beş dashboard metnini fail-closed denetliyor. Canlı dashboard read-only kontrolü Google yeniden kimlik doğrulaması beklediği, `0.1.38` ZIP yükleme ve inceleme ise kullanıcı hesabı/onayı gerektirdiği için açık kalır.
- Sonraki adım: Canlı öğeyi yeni kayıt oluşturmadan aynı `iaikobkolclhhpcogacjkenijlfaibpf` kimliğiyle koru. `v0.1.38` exact-tag paketi hazırlandıktan sonra dashboard'da gizlilik politikasını `https://mcolaker.github.io/BookmarkFlow-Bar/privacy/`, destek adresini `https://github.com/mcolaker/BookmarkFlow-Bar/issues`, veri türlerini **Web history** ve **Website content** olarak doğrula; yalnız ZIP'i yükleyip yeniden incelemeye gönder. Canlı sürüm ve alanların read-only kanıtı ile yeni inceleme kullanıcı hesabı/onayı gerektirdiğinden görev `BLOCKED` kalır.
- Son güncelleme: 2026-08-05.

## BF-PRIV-001 - İlk kullanım veri erişimini açık onayla sınırla

- Öncelik ve durum: P0, DONE.
- Kök neden ve kanıt: 2026-08-05 kaynak/politika karşılaştırması `background.js`, onboarding, content script, new-tab, popup ve klasör bakım ekranının açık kullanıcı onayından önce yer imi ağacı, sayfa bağlamı veya kullanıcı tercihlerini işleyebildiğini gösterdi; eski `bfOnboardingSeen` değeri belirgin açıklama sonrası verilmiş sürümlü rıza değildi. Yeni-tab yatay kaydırma konumu ayrıca kalıcı olarak kaydediliyor ve dashboard'daki “User activity” seçilmemeli beyanıyla gereksiz bir gri alan oluşturuyordu. Chrome Web Store'un 1 Ağustos 2026'da yürürlüğe giren genişletilmiş prominent-disclosure kuralı nedeniyle bu davranış yeni paket öncesi fail-closed kapatılmalıdır.
- Kabul kriteri: Sürümlü ve yalnız yerel `bfDataConsentVersion` değeri eksik, bozuk veya eskiyse bookmark, page-context, tercih migrasyonu ve Search API akışları çalışmaz; onboarding veri türlerini, amaçları, Chrome Sync/varsayılan arama sağlayıcısı sınırını ve geliştiricinin veri almadığını açıkça gösterip ayrı bir onay eylemi ister; diğer bütün yüzeyler onay verilene kadar inert kalır; gereksiz yeni-tab scroll kaydı kaldırılır.
- Doğrulama kapısı: Onay öncesi state ve mutasyon mesajları sıfır bookmark okuma/yazmayla reddedilir; content script sayfaya stil/DOM eklemez; popup, new-tab ve bakım yüzeyi yalnız gizlilik kurulum rotasını sunar; İngilizce/Türkçe anahtar paritesi, proje/public-tree ve gerçek geçici Chrome iki dil regresyonu geçer; onay sonrası normal davranış, onay kaldırıldığında yeniden kilitlenme kanıtlanır. 2026-08-05 runtime/UI/paket sözleşmeleri 23/23 geçti; İngilizce ve Türkçe gerçek geçici Chrome koşuları onay öncesi state/content/new-tab/popup/bakım yüzeylerini inert, güvenilir tıklamayla onayı başarılı, normal bookmark/Search API davranışını çalışır ve yetkili mesajla onay kaldırıldıktan sonra bütün yüzeyleri yeniden kilitli kanıtladı. Yeni-tab scroll anahtarı kaynak ve QA sözleşmesinden kaldırıldı; proje doğrulaması ve 96 dosyalık public-tree taraması temizdir.
- Sonraki adım: Yok; veri kapsamı veya işleme amacı değişirse consent sürümünü artır, açıklama ile dashboard beyanlarını birlikte güncelle ve aynı negatif/iki-dilli gerçek Chrome matrisini yeniden çalıştır.
- Son güncelleme: 2026-08-05.

## BF-UX-001 - Sayfa ve tarayıcı klavye kısayollarını güvenli sınırla

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: 2026-08-04 yeniden üretiminde capture-phase document dinleyicisi BookmarkFlow kapalı, devre dışı hostta veya hassas hostta render edilmese bile `Ctrl+K` ve `Alt+Space` olaylarında `preventDefault()` ve `stopPropagation()` çağırdı. Manifestteki `Ctrl+Shift+E` varsayılanı da kullanıcının AMD yazılımıyla çakışıyor; onboarding/README/GIF metinleri gerçek `chrome.commands` atamasını dinamik okumuyor.
- Kabul kriteri: Sayfa içi alias'lar yalnız BookmarkFlow görünür ve etkin olduğunda, düzenlenebilir hedef dışında çalışır; kapalı/global-disabled/disabled-host/sensitive-host senaryolarında sayfa ve Chrome kısayolları tüketilmez; varsayılan arama komutu daha düşük çakışmalı bir kombinasyon kullanır ve kullanıcıya gösterilen kısayol mümkün olan her yüzeyde `chrome.commands.getAll()` sonucundan gelir.
- Doğrulama kapısı: Olay iptal durumunu ölçen negatif klavye testleri bütün görünürlük/host/editable matrisinde geçer; manifest komut sözleşmesi, onboarding/popup metni, README ve hareketli tur açıklaması aynı davranışı gösterir; gerçek Chrome reload ve klavye smoke kanıtı temizdir. Otomatik negatif alias, manifest ve dinamik komut sunumu kontratları geçti; son kullanıcı profilindeki reload ve fiziksel kısayol matrisi `not_run` durumundadır.
- Sonraki adım: Sayfa alias'ları kaldırıldı, varsayılan `Alt+Shift+K` yapıldı, onboarding ve popup gerçek `chrome.commands.getAll()` atamasını gösteriyor ve negatif UI sözleşmesi geçti. Kullanıcı Chrome'da uzantıyı reload edip `Ctrl+K`, `Alt+Space`, atanmış arama komutu ve `chrome://extensions/shortcuts` yeniden atama akışını doğrulasın; gerçek klavye kanıtına kadar görev `BLOCKED` kalır.
- Son güncelleme: 2026-08-04.

## BF-DATA-001 - Klasör renklerini profil-yerel kimliklerle güvenli sakla

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: Klasör renkleri bookmark node ID anahtarıyla genel ayar nesnesine yazılıyor ve `chrome.storage.sync` üzerinden cihazlar arasında taşınıyordu. Chrome bookmark ID'leri yalnız mevcut profil içinde benzersiz olduğundan başka cihazda renk kaybolabilir veya aynı ID'li farklı klasöre uygulanabilir.
- Kabul kriteri: `folderColors` yalnız `chrome.storage.local` içinde saklanır; mevcut sync verisi tek seferlik ve veri kaybetmeyen migrasyonla yerel depoya alınır; diğer senkronize ayarlar ve profil-yerel pinned-folder davranışı değişmez; migration idempotenttir.
- Doğrulama kapısı: Eski sync-only, yeni local-only, çakışan local+sync ve ikinci çalıştırma migration testleri geçer; sync yazımında `folderColors` bulunmaz; proje/public-tree/güvenlik regresyonları temizdir. Runtime sözleşmesindeki başarılı migrasyon ve yerel yazma hatasında veri koruma senaryoları geçti; gerçek Chrome regresyonu `profileLocalFolderColors: pass` verdi ve son proje/public-tree kapıları temizdir.
- Sonraki adım: Yok; `folderColors` yerel depoya taşındı, eski sync verisi yerel değerleri ezmeden tek seferlik migrate ediliyor ve yerel yazma hatasında eski veri korunuyor. Sync/local/çakışma/hata sözleşmeleri ve gerçek Chrome güvenlik regresyonu temizdir.
- Son güncelleme: 2026-08-04.

## BF-A11Y-001 - Modal ve arama yüzeylerinde odak/semantik sözleşmesini tamamla

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: 2026-08-04 kaynak denetimi sayfa içi ve new-tab ekleme overlay'lerinde eksik `role=dialog`/`aria-modal`, odak tuzağı ve kapanışta odağı geri döndürme davranışı; arama panelinde ise `aria-selected` kullanıldığı halde eşleşen listbox/option semantiğinin bulunmadığını gösterdi.
- Kabul kriteri: Modal yüzeyler erişilebilir ad, dialog/modal semantiği, ilk odak, Tab/Shift+Tab çevrimi, Escape ile güvenli kapanış ve tetikleyiciye odak dönüşü sağlar; arama sonuçları tutarlı listbox/option veya eşdeğer erişilebilir örüntü kullanır; mevcut kapalı Shadow DOM ve görsel tasarım korunur.
- Doğrulama kapısı: Kaynak kontrat testleri ve klavye davranış testleri semantik/odak/Escape/dönüş akışını geçer; açık/koyu arka plan, ilgili viewport ve gerçek Chrome reload sırasında klavye ile bütün akış tamamlanır. Kaynak/UI sözleşmeleri dialog, odak tuzağı, Escape, odak dönüşü ve combobox/listbox modelini geçti; açık/koyu gerçek kullanıcı profili viewport kanıtı `not_run` durumundadır.
- Sonraki adım: Dialog/modal/combobox/listbox semantiği, inert arka plan, Tab/Shift+Tab tuzağı, Escape kapanışı ve odak dönüşü uygulandı; otomatik UI sözleşmeleri geçti. Açık ve koyu arka planda, masaüstü ve dar viewportta gerçek Chrome klavye akışı tamamlanıp görünür odak kanıtı alınana kadar görev `BLOCKED` kalır.
- Son güncelleme: 2026-08-04.

## BF-GOV-002 - Katkı topluluğu davranış standardını yayımla

- Öncelik ve durum: P2, DONE.
- Kök neden ve kanıt: Herkese açık repo katkı rehberi ve özel güvenlik bildirim kanalı içeriyor ancak katılımcılardan beklenen davranış, özel ihlal bildirimi, gizlilik, yaptırım sorumluluğu ve kademeli müdahale standardını tek bir kamusal sözleşmede tanımlamıyordu. Contributor Covenant 3.0 temel alınarak proje kapsamına uyarlanmış `CODE_OF_CONDUCT.md` eklendi; README ve katkı rehberi belgeye bağlandı, `.gitignore` allowlist'i ve proje doğrulayıcısı yeni zorunlu yönetişim dosyasını kapsayacak şekilde güncellendi.
- Kabul kriteri: Contributor Covenant 3.0 temel alınarak BookmarkFlow Bar kapsamına uyarlanmış İngilizce `CODE_OF_CONDUCT.md` yayımlanır; beklenen ve yasak davranışlar, kapsam, özel bildirim kanalı, güvenlik bildirimi ayrımı, gizlilik, misilleme yasağı, yaptırım basamakları ve CC BY-SA 4.0 atfı açıkça tanımlanır; README ve katkı rehberi belgeye bağlanır.
- Doğrulama kapısı: `node scripts/validate-backlog.mjs`, 5/5 backlog sözleşme testi, yedi zorunlu sunum dosyasını kapsayan `validate-project`, 68 dosyalık public-tree taraması ve `git diff --check` temizdir. PR #6 `validate` kontrolü terminal `success` verdi ve `ca17c3c8fe49342d2fb83c6a59a4fac5a6089367` merge commit'iyle `main` dalına birleştirildi; aynı commit için ana dal `validate` çalışması da terminal `success` verdi.
- Sonraki adım: Yok; gelecekte topluluk iletişim kanalı veya moderasyon sorumlusu değişirse belgeyi, bağlantıları ve özel bildirim rotasını birlikte güncelle.
- Son güncelleme: 2026-08-02.

## BF-GOV-003 - GitHub Discussions ve destek katılım yolunu yayımla

- Öncelik ve durum: P2, DONE.
- Kök neden ve kanıt: Herkese açık repo Issues, katkı rehberi ve güvenlik politikası içeriyor ancak topluluk soruları ile fikirlerini iş kapsamına alınmış bir issue açmadan konuşabilecekleri GitHub Discussions kapalıydı; README desteği de katkı kanallarını ve kaynak-görünür lisans sınırlarını tek bir bölümde açıklamıyordu.
- Kabul kriteri: Repo düzeyinde GitHub Discussions etkinleştirilir; README yıldız, sürüm takibi, Discussions, Issues, test, erişilebilirlik, yerelleştirme, dokümantasyon ve paylaşım yollarını açıkça sunar; güvenlik bildirimlerini özel kanala yönlendirir; fork ve katkıların `LICENSE.md` ile `CONTRIBUTING.md` koşullarına tabi olduğunu belirtir.
- Doğrulama kapısı: GitHub repo API'si `has_discussions: true` döndürdü ve herkese açık Discussions rotası HTTPS `200` verdi. Backlog doğrulaması, 5/5 sözleşme testi, `validate-project`, 68 dosyalık public-tree taraması, güvenlik regresyonu ve `git diff --check` temizdir. PR #8 `validate` kontrolü terminal `success` verdi ve `855d3a1d85a9c6a733c70a2a7752f87dbf2daf7f` merge commit'iyle `main` dalına birleştirildi; aynı commit için ana dal `Validate extension` çalışması `30755465039` ve Pages çalışması `30755464506` terminal `success` verdi.
- Sonraki adım: Yok; yeni topluluk kanalı eklenirse README yönlendirmelerini, lisans sınırlarını ve fail-closed destek doğrulamasını birlikte güncelle.
- Son güncelleme: 2026-08-02.

## BF-GOV-004 - Kanonik GitHub sahibi ve Pages bağlantılarını uzlaştır

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: GitHub repo API'si kanonik sahibi `mcolaker` olarak döndürürken kaynak ve mağaza belgelerinde önceki `09mc` sahibi kalmıştı; GitHub repo bağlantıları yönlendirilse de `https://09mc.github.io/BookmarkFlow-Bar/` 404, `https://mcolaker.github.io/BookmarkFlow-Bar/` ise 200 döndü. Pages dağıtımı da geçiş sırasında eski OIDC audience değeri nedeniyle başarısız oldu. Kaynak bağlantıları düzeltildikten ve görev kapatıldıktan sonra 2026-08-02 tarihli imzasız ziyaretçi denetimi GitHub `About` alanındaki uzak `homepage` ayarının hâlâ `https://09mc.github.io/BookmarkFlow-Bar/` olduğunu ve kullanıcıyı 404 sayfasına gönderdiğini kanıtladı; görev regresyon kanıtıyla yeniden açıldı.
- Kabul kriteri: Manifest, README, yönetişim belgeleri, issue şablonu, Pages canonical/Open Graph bağlantıları, mağaza metinleri, GitHub `About` homepage ayarı ve doğrulayıcı kanonik `mcolaker` repo/Pages adreslerini kullanır; geçmiş kanıt kaydı dışında eski `09mc` sahibi canlı bağlantı hedefi olarak kalmaz.
- Doğrulama kapısı: `validate-project` canlı bağlantı yüzeylerinde eski repo/Pages URL'lerini fail-closed reddediyor. Repo API'si `mcolaker/BookmarkFlow-Bar`, `has_discussions: true`, `public` görünürlük ve `homepage: https://mcolaker.github.io/BookmarkFlow-Bar/` döndürdü; kanonik Pages rotası imzasız HTTPS isteğinde `200` verdi. Yerel backlog doğrulaması, 5/5 sözleşme testi, `validate-project`, 80 dosyalık public-tree taraması, güvenlik regresyonu ve `git diff --check` temizdir. Regresyon düzeltmesini içeren PR #19'un `Validate extension` çalışması `30761006773` terminal `success` verdi ve PR `4335eedf9f59986574937bccdbad803aa6a59d29` merge commit'iyle `main` dalına birleştirildi; aynı commit için ana dal `Validate extension` çalışması `30761067634` ve Pages çalışması `30761067242` terminal `success` verdi.
- Sonraki adım: Yok; Chrome Web Store dashboard URL uzlaştırması `BF-STORE-001` kapsamında açık tutulur.
- Son güncelleme: 2026-08-02.

## BF-GOV-005 - GitHub ziyaretçi dönüşümü ve topluluk girişini güçlendir

- Öncelik ve durum: P1, DONE.
- Kök neden ve kanıt: 2026-08-02 tarihli imzasız masaüstü denetiminde README'nin ürün değerini ve gerçek arayüz kanıtını güçlü sunduğu, ancak doğrulanmış sürüm indirme yolunun ilk görünümde bulunmadığı ve kurulum bölümüne geç ulaşıldığı görüldü. Discussions açık olmasına rağmen sıfır gönderi ve varsayılan kategori yapısı topluluk girişini boş bırakıyor; GitHub profili profesyonel bio ve küratörlü sabit repo içermiyor; `good first issue` ve `help wanted` etiketleri bulunmasına rağmen katkıya hazır açık iş yok. `docs/assets/bookmarkflow-hero.jpg` 1280x640, 72.121 bayt, kaynağı belgeli ve kişisel veri içermeyen mevcut sosyal önizleme adayıdır.
- Kabul kriteri: README ilk görünümde doğrulanmış `v0.1.37` paketi, checksum, kurulum, ürün sitesi ve Discussions rotalarını; tarihli proje durumunu ve kısa gizlilik/izin kanıtını sunar. GitHub sosyal önizlemesi belgeli hero varlığını kullanır; Discussions'ta sabitlenmiş karşılama/sürüm gönderisi ile iş akışı ve kurulum geri bildirim yolları bulunur; gerçek roadmap kapsamından en az iki katkı issue'su uygun `good first issue` veya `help wanted` etiketleriyle açılır; GitHub profili yeni herkese açık repo oluşturmadan profesyonel bio ve yalnız BookmarkFlow Bar sabitlemesiyle güncellenir; web commit sign-off gereksinimi etkindir.
- Doğrulama kapısı: Backlog, açık kaynak, DCO, proje, 80 dosyalık public-tree ve güvenlik regresyonu doğrulamaları temizdir; `git diff --check` geçer. README ilk görünümü doğrulanmış `v0.1.37` ZIP/checksum, kurulum, ürün sitesi ve Discussions rotalarını sunar. Sosyal önizleme izlenen 1280x640 hero dosyasıyla birebir eşleşir (SHA-256 `30075d84d92ba9a2d291e9e79537676ccd0685f4223d96a059df3bfb343ffc17`). Discussions #14 sabit karşılama, #15 iş akışı vitrini ve #16 kurulum geri bildirimi olarak yayımlandı. Issues #17 `enhancement` + `help wanted`, #18 `enhancement` + `good first issue` etiketleriyle açıktır. İmzasız profil bio'yu, proje avatarını ve yalnız `Pinned` BookmarkFlow Bar kartını gösterir; yeni profil deposu oluşturulmadı. Repo API'si `web_commit_signoff_required: true` döndürür. PR #19'un `Validate extension` çalışması `30761006773` terminal `success` verdi ve PR `4335eedf9f59986574937bccdbad803aa6a59d29` merge commit'iyle korumalı `main` dalına birleştirildi; ana dal doğrulaması `30761067634` ve Pages dağıtımı `30761067242` terminal `success` verdi.
- Sonraki adım: Yok; kullanıcı geri bildirimi ve dış katkılar Discussions ile #17-#18 üzerinden izlenir.
- Son güncelleme: 2026-08-02.

## BF-MKT-001 - Global görsel ve hareketli tanıtım setini profesyonelleştir

- Öncelik ve durum: P1, BLOCKED.
- Kök neden ve kanıt: 2026-08-02 tarihli güncel varlık denetiminde hero güçlü bulunurken mağaza promosunda küçük/kırpılmış ürün kadrajı, new-tab yakalamasında görünür scrollbar ve aşırı boş alan, overlay yakalamasında sayfa içeriğiyle çarpışma, İngilizce README içindeki GIF'lerde Türkçe metinler, panel arkasından görünen demo sayfası yazıları, görünmeyen etkileşim tetikleri ve sonsuz döngü tespit edildi. Üretim altyapısı beş İngilizce ve beş Türkçe ekran görüntüsü, iki dilde promo/marquee, güvenli boşluklu beş ikon ve İngilizce beş tek oynatımlı GIF üretecek şekilde düzeltildi. Bağımsız son inceleme, mevcut `search-palette.gif` karesinde arama alanının sol kenarının ve `context-actions.gif` karesinde menünün alt sınırının dar kadrajlandığını gösterdi; üretici tam genişlik kadrajı, DOM sınır kontrolü ve hedef boyuta kayıpsız ölçekleme kullanacak şekilde düzeltildi ancak bu iki binary henüz yeniden üretilmedi. Kullanıcının manuel reload işlemi sonrasında onaylı Chrome yüzeyinde `https://example.com/` üzerinde dinamik `bookmarkflow-bar-root-*`, sayfa stili ve görünür kompakt `BF` kontrolü doğrulandı; böylece gerçek profilde content-script yüklenmesi geçti. Buna karşılık `chrome-extension://…/src/newtab.html` erişimi `Browser Use rejected this action due to browser security policy` engeline takıldı; politika gereği alternatif tarayıcı/CDP/Playwright erişimiyle bu kanıt aşılmadı. 2026-08-04 fail-closed düzeltmesinde eski `Ctrl+Shift+E` ipucunu ve dar kadrajı taşıyan iki binary README ile onboarding'den kaldırıldı ve `.gitattributes` ile release arşivinden dışlandı; köken kaydı bunları açıkça `refresh pending` olarak işaretliyor. 2026-08-05 kullanıcı onayıyla uzantı paketinden ayrı `media/promo-video/` çalışma alanı kuruldu; tam sürüm, X ve 4:5 teaser kompozisyonları, İngilizce SRT dosyaları, tek oynatımlı GitHub GIF'i, poster, sentetik 1920x1080 gerçek-uzantı yakalama aracı ve deterministik yerel ses yatağı eklendi. Kişisel profil yerine geçici en-US Chrome profili ve sentetik yer imleri kullanıldı; uzak istekler fail-closed reddedildi.
- Kabul kriteri: Koyu lacivert/altın BookmarkFlow tasarım sistemi korunarak scrollbar ve içerik çarpışması olmayan beş İngilizce mağaza ekran görüntüsü, Türkçe mağaza varyantları, daha az metinli ve küçük ölçekte okunaklı 440x280 promo, 1400x560 marquee, güvenli boşluklu kanonik ikon ailesi ve temiz sentetik arka plan üzerinde İngilizce üretilmiş beş hareketli tur varlığı hazırlanır; README medya ölçüleri gerçek varlık ölçüleriyle eşleşir ve hareket açıklamaları erişilebilir biçimde sunulur.
- Doğrulama kapısı: Üreticiler sabit sentetik veriyle ve staging sonrası geri-alma korumalı toplu yayınla çalışacak şekilde sertleştirildi; içerik komutu yanıtı, locale, favicon, taşma, arama sonucu, DOM'dan türetilen UI durumu ve kadraj sınırı fail-closed denetleniyor. Binary kapısı izlenen ve git tarafından yok sayılmayan tüm PNG/JPEG/GIF dosyalarını tam envanterle eşleştiriyor; zorunlu boyut, EN/TR ayrımı, en fazla beş saniyelik tek oynatım ve SHA-256/provenance eşleşmesi geçiyor. README gerçek medya genişliklerini, etkileşim ipuçlarını ve metinsel hareket özetlerini sunuyor. 2026-08-05 medya render kapısı 58 saniyelik master/LinkedIn, 32 saniyelik X ve 15 saniyelik 4:5 teaser çıktılarında H.264 High, yuv420p, BT.709, 30 fps ve AAC-LC 48 kHz stereo sözleşmesini; poster, 4,58 saniyelik tek oynatımlı GIF, 8/5 cue İngilizce SRT, manifest ve SHA-256 özetini doğruladı. Kullanıcının karanlık/gerilimli bulduğu ilk yerel ses yatağı teslimden çıkarıldı; yerine 104 BPM D-A-G-A majör ilerlemeli, aydınlık elektronik ve tamamı proje koduyla sentezlenen parça kondu. Yeni master ortalama `-22.9 dB`, tepe `-8.9 dB` ve clip olmadan doğrulandı; ana master SHA-256 değeri `fc9a5fdb422ffa72a4b48f1eef6295eb5a9e083bf47b66c1dde3c954df4972a2`, GitHub GIF'i `81eb19948e96572ca4cb3ec88aad0586aa27a0022b48be1a7adbb890115d3c9e`, poster JPEG'i `54ed70aece14899308a83a2056fb9f811509efdd6281cbf25c0b2f647363a649` olarak kaydedildi. Poster ve sekiz sahneli contact sheet orijinal çözünürlükte incelendi; kişisel veri, üçüncü taraf işareti, scrollbar, güvensiz kırpma veya taşma görülmedi. MP4/SRT teslimleri `PREPARED_NOT_UPLOADED`; medya alanı ve büyük çıktılar Chrome ZIP'inden dışlanır. Gerçek Chrome profilinde reload/content-script/kompakt kontrol görünürlüğü `pass`; genişletilmiş açık sayfa, koyu yeni sekme paleti ve dar kadrajlı iki GIF'in yeni üreticiyle yeniden üretilmesi `not_run` durumundadır. Bu kanıtlar gelmeden görev `DONE` yapılmaz.
  - 2026-08-05 final kapanış kanıtı: backlog kontratı 5/5, açık kaynak/DCO kontratı 14/14, runtime/UI kontratı 17/17, paket kontratı 6/6, proje doğrulaması manifest `0.1.38` / 25 JavaScript / 2 locale / 19 presentation / 27 binary ve public-tree taraması 113 dosya olarak temizdir; gerçek Chromium güvenlik regresyonu ve `git diff --check` geçmiştir.
  - 2026-08-05 sosyal teslim kanıtı: Onaylı digestlerden 4 MP4, 2 İngilizce SRT, tek oynatımlı README GIF'i, 5 İngilizce mağaza ekran görüntüsü, LinkedIn/X/Reddit/DEV/Product Hunt için 5 oran-özel görsel ve checksum'lu İngilizce launch-copy/posting-guide üreten fail-closed `prepare-social-kit.mjs` eklendi. Bağımsız denetim sonrası manifest mevcut assembly commit'i, filmin gerçek `ef1fe20` kaynak commit'i, promo manifest ve script digestlerini ayrı kaydedecek şekilde düzeltildi; FFmpeg/FFprobe `8.0.1`, golden PNG hash'leri ve klasör düzeyinde staging/swap/rollback zorunlu hale getirildi. İki ardışık üretim aynı 19 artefaktı verdi; manifest `PREPARED_NOT_POSTED`, kişisel/uzak veri `forbidden`, tüm kopya/hash/boyut/codec ve SHA-256 readback kapıları geçti. Beş oran-özel görsel orijinal çözünürlükte incelendi; metin, logo, güvenli boşluk, kadraj ve kişisel/üçüncü-taraf içerik kontrolleri temizdir. Kullanıcı tam filmi GitHub'a yüklemeyi açıkça onayladı; LinkedIn/X hesap gönderileri ayrıca açık yayın talimatı gelene kadar yapılmaz.
- Sonraki adım: PR #21'i yeşil CI kanıtıyla merge commit olarak birleştir, değişmez `v0.1.38` etiketinden GitHub Release'i yayımla ve ana MP4/SRT/checksum varlıklarını Chrome ZIP'inden açıkça ayrı yükle. Ardından gerçek Chrome profilinde genişletilmiş açık sayfa ve koyu yeni sekme görünümünü doğrula; `search-palette.gif` ile `context-actions.gif` dosyalarını düzeltilmiş üreticiyle yeniden üretip orijinal çözünürlükte incele. LinkedIn/X için hazırlanmış `PREPARED_NOT_POSTED` kesitleri ayrı kullanıcı onayı olmadan yayımlanmaz.
- Son güncelleme: 2026-08-05.

## BF-GOV-006 - GitHub profil sosyal bağlantılarını doğrula ve yayımla

- Öncelik ve durum: P2, DONE.
- Kök neden ve kanıt: Profesyonel bio ve BookmarkFlow Bar sabitlemesi tamamlanmış olmasına rağmen GitHub profilinin Social accounts alanında LinkedIn ve X bağlantıları boştu. Mevcut oturumlu LinkedIn yüzeyi hesap sahibini `Muhammed Çolaker`, profil hedefini `https://www.linkedin.com/in/mcolaker/`; mevcut oturumlu X yüzeyi hesap adını `ForeverLife`, kullanıcı adını `@Foreverrlifeee` ve profil hedefini `https://x.com/Foreverrlifeee` olarak gösterdi. Bu iki doğrulanmış HTTPS bağlantısı GitHub profil ayarlarına eklendi; GitHub `Profile updated successfully` sonucu verdi.
- Kabul kriteri: Yalnız kullanıcıya ait olduğu mevcut oturum, kamusal çapraz bağlantı veya kullanıcının açık teyidiyle doğrulanan LinkedIn ve X HTTPS profil URL'leri GitHub profilinin sosyal hesaplar alanına eklenir; mevcut profesyonel bio, konum, website ve sabit repo kapsamı bozulmaz; gereksiz yeni repo oluşturulmaz.
- Doğrulama kapısı: Oturum sahibi GitHub profilinin herkese açık görünümü LinkedIn ikonunu `https://www.linkedin.com/in/mcolaker/`, X bağlantısını `@Foreverrlifeee` etiketiyle `https://x.com/Foreverrlifeee` hedeflerine bağlı gösterdi; mevcut bio, `https://www.colaker.net`, `Türkiye` konumu ve BookmarkFlow Bar sabitlemesi korundu. Tahmini kullanıcı adı, e-posta veya hassas bilgi eklenmedi.
- Sonraki adım: Yok; sosyal kullanıcı adı veya marka hesabı değişirse oturumdan yeniden doğrula ve iki hedefi birlikte güncelle.
- Son güncelleme: 2026-08-02.
